import React from "react";

import { SendMessageStyle } from "../styles/Message";

const message = "{ send message }";

const SendMessage = () => {
  return <SendMessageStyle>{message}</SendMessageStyle>;
};
export default SendMessage;
